// Note: This is default prettier configuration

/** @satisfies {import("prettier").Config} */
const config = {
  printWidth: 80,
  tabWidth: 4,
  useTabs: false,
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};

export default config;
